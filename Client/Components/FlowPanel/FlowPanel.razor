@namespace FileFlows.Client.Components

@using FileFlows.Plugin

@foreach (var field in Fields?.OrderBy(x => x.Order)?.ToList() ?? new List<ElementField>())
{
    <InputRenderer field="field" OnSubmit=@OnSubmit OnClose=@OnClose />
}