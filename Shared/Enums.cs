namespace FileFlows.Shared
{
    public enum OperatingSystemType
    {
        Unknown = 0,
        Windows = 1,
        Linux = 2,
        Mac = 3
    }

    public enum FlowType
    {
        Standard = 0,
        Failure = 1
    }
}
